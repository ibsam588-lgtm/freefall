// systems/obstacle_system.dart
//
// Phase 1 placeholder. Phase 2 will spawn obstacles below the camera,
// recycle them as they scroll off the top, and feed their AABBs into
// the CollisionSystem each tick.

import 'system_base.dart';

class ObstacleSystem implements GameSystem {
  @override
  void update(double dt) {
    // No-op until obstacle spawning lands.
  }
}
