// systems/coin_system.dart
//
// Phase 1 placeholder. Phase 2 will spawn coins along forgiving paths,
// run pickup detection through CollisionSystem, and emit collect FX.

import 'system_base.dart';

class CoinSystem implements GameSystem {
  @override
  void update(double dt) {
    // No-op until coin spawning lands.
  }
}
